// Tuples
let tuple: [number, string, boolean] = [12, "Vijay", true];

// tuple[1] = true; ------ not allowed

let tupleExample: [string, number, string];
// Although the first element of the array was expected to be a string, but it is an integer 
// (This is an issue with using push method in tuples)
// tupleExample.push(1);


